{
  "name": "swc-plugin-replace-t",
  "private": true,
  "version": "0.2.0",
  "description": "",
  "author": "",
  "license": "ISC",
  "keywords": [
    "swc-plugin"
  ],
  "main": "./target/wasm32-wasi/release/swc_plugin_replace_t.wasm",
  "files": [
    "./target/wasm32-wasi/release/swc_plugin_replace_t.wasm"
  ],
  "scripts": {
    "build-wasm": "cargo build-wasi --release",
    "lint": "cargo fmt --all -- --check && cargo clippy -- -D warnings",
    "test": "cargo test"
  },
  "devDependencies": {
    "@swc/cli": "0.1.59",
    "@swc/core": "1.3.24"
  }
}
